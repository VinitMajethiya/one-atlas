import { ReactNode } from 'react';

export const metadata = {
  title: 'Search Blueprints — OneAtlas',
  description: 'Search and filter active blueprints in our operational registry.',
};

export default function SearchLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
