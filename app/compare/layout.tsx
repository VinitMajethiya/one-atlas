import { ReactNode } from 'react';

export const metadata = {
  title: 'Compare Templates — OneAtlas',
  description: 'Compare templates side by side across features, integrations, and health scores.',
};

export default function CompareLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
