import { ReactNode } from 'react';

export const metadata = {
  title: 'AI Schema Generator — OneAtlas',
  description: 'Instantly generate design schemas and database configurations using AI.',
};

export default function GenerateLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
