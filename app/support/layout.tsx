import { ReactNode } from 'react';

export const metadata = {
  title: 'Help & Support — OneAtlas',
  description: 'Get answers to your deployment, schema, and SSO integration queries.',
};

export default function SupportLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
