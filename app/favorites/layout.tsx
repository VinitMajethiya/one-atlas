import { ReactNode } from 'react';

export const metadata = {
  title: 'Bookmarked Blueprints — OneAtlas',
  description: 'View your collection of saved operational app templates.',
};

export default function FavoritesLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
